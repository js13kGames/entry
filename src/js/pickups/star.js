import { Pickup } from './pickup.js';

export class StarPickup extends Pickup {

    constructor(props) {
        super(props);
    }

    render() {


        // Render the circle around the edge
        //super(props);
    }

}
