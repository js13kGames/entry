
export class Player {
    constructor(color, ship, controls) {
        this.color = color || '#fff';
        this.controls = controls;
        this.ship = ship || 'tri';
        this.ready = false;
    }

    update() {

    }

    respawn() {

    }
}
