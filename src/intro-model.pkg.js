const initIntroModel = () => {
  const buttonText = 'Start!';
  
  const title = 'Back to Skull Island';

  const instructions = 'Here is how to play the game.';

  return {
    buttonText,
    instructions,
    title
  };
};
